{
  "name": "presentation-demystifying-webrtc",
  "version": "1.0.0",
  "private": true,
  "description": "A technical talk explaining why WebRTC exists and what protocols make it work.",
  "engines": {
    "node": "24.18.0"
  },
  "scripts": {
    "deck:sync": "node scripts/deck-metadata.mjs",
    "deck:check": "node scripts/deck-metadata.mjs --check",
    "snippets": "node scripts/build-snippets.mjs",
    "css": "node scripts/build-theme.mjs",
    "prepare:deck": "npm run deck:check && npm run snippets && npm run css",
    "prelint": "npm run prepare:deck",
    "lint": "node scripts/check-slides.mjs",
    "prepreview": "npm run prepare:deck",
    "preview": "marp --server . --html",
    "prehtml": "npm run prepare:deck",
    "html": "marp slides.md --html --theme themes/base.css -o dist/index.html",
    "posthtml": "node scripts/copy-assets.mjs",
    "prepdf": "npm run prepare:deck",
    "pdf": "marp slides.md --pdf --allow-local-files --theme themes/base.css -o dist/slides.pdf",
    "prepptx": "npm run prepare:deck",
    "pptx": "marp slides.md --pptx --allow-local-files --theme themes/base.css -o dist/slides.pptx"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.3.0",
    "sass": "^1.101.0"
  }
}
